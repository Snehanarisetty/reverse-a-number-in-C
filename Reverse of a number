#include <stdio.h>
int main() {
  int rem,n,sum = 0;
  printf("Enter an integer: ");
  scanf("%d", &n);
  while (n != 0) {
    rem = n % 10;
    sum = sum * 10 + rem;
    n /= 10;
  }
  printf("%d",sum );
  return 0;
}
